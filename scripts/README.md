# Scripts

Ad-hoc one-off scripts used to live here.
Current eval entrypoints:

- FinanceBench: `evals/financebench/run_answers.py` (see `AGENTS.md`)
- Promptfoo wrapper: `evals/run.py` (needs private `ven_eval`)

